name: Build and Bundle Application

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - name: Set up GraalVM
        uses: actions/setup-java@v5
        with:
          java-version: '25'
          distribution: 'graalvm'
          cache: maven

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: ui/package-lock.json

      - name: Build Frontend
        run: |
          cd ui
          npm ci
          npm run generate-api
          npm run build
          npm test -- --watch=false --browsers=ChromeHeadless

      - name: Bundle Frontend into Backend
        run: |
          mkdir -p src/main/resources/static
          cp -r ui/dist/precrime-ui/* src/main/resources/static/

      - name: Build Backend JAR and Run Tests
        run: |
          chmod +x mvnw
          ./mvnw clean package

      - name: Upload JAR Artifact
        uses: actions/upload-artifact@v7
        with:
          name: pre-crime-app
          path: target/pre-crime-app.jar
          if-no-files-found: error

      - name: Build Native Linux Executable
        run: |
          ./mvnw -Pnative package -DskipTests

      - name: Upload Native Executable
        uses: actions/upload-artifact@v7
        with:
          name: pre-crime-app-linux
          path: target/pre-crime-app
          if-no-files-found: error

  e2e:
    needs: build
    uses: ./.github/workflows/playwright-e2e.yml
